## Quick start (dev)

1. Clone repo into your server resources folder (or symlink).
2. Ensure in `server.cfg`:
   - `ensure rpstack-core`

Then verify core loads by checking console logs for:
`[RPStack] ... rpstack-core ready`
